import 'package:flutter/foundation.dart';
import 'package:flutter/widgets.dart';

class MapForOrder extends StatefulWidget {
  const MapForOrder({super.key, });

  @override
  State<MapForOrder> createState() => _MapForOrderState();
}

class _MapForOrderState extends State<MapForOrder> {
  @override
  Widget build(BuildContext context) {
    //map for one order
    return Container();
  }
}